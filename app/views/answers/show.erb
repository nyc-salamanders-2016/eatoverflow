<h1>the show answer page</h1>

 <span <%= "Written on #{long_form_date(@answer.created_at)}." %></span>

 <span><%= "Written on #{long_form_date(@answer.created_at)} by #{find_user(@answer.user_id)}." %></span>

<p><%= @answer.body %></p>

<% if logged_in? && is_current_user?(@answer.user_id) %>
<ul>
  <li>
    <a href="/answers/<%= @answer.id %>/edit">edit</a>
  </li>
  <li>
    <form method="post" action="/answers/<%= @answer.id %>" class="inline">
      <input type="hidden" name="_method" value="delete">
      <input type="submit" value="delete">
    </form>
  </li>
</ul>
<% end %>
=======
<ul class="no-bullets no-padding mar-t-2">
    <li class="pipe-separate left">
      <a href="/entries/<%= @answer.id %>/edit" class="c-white">edit</a>
      </form>
    </li>
  </ul>
>>>>>>> added comment form and routs for questions and answers
